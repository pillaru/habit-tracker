@model IEnumerable<HabitTracker.Domain.Habit>

<h1>Habit Tracker</h1>

<a href="/habits/new">Start a new habit</a>

@if (Model.Count() == 0)
{
    <p>No habits found.</p>
}

@foreach(var habit in Model)
{
    <p>@habit.Title</p>
}
